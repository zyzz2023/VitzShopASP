@page "{id:Guid}"
@model ProductCardModel
@{
    ViewData["Title"] = Model.ProductDto?.Name ?? "Загрузка...";
}

@if (Model.ProductDto == null)
{
    <p>Загрузка...</p>
}
else
{
    @* <div class="container mt-1">
        <div class="row flex-column flex-md-row">
            <!-- Карусель изображений -->
            <div class="offset-md-1" style="max-width: 500px; margin-left: 0;">
                <div id="productCarousel" class="carousel slide">
                    <div class="carousel-inner">
                        @for (int i = 0; i < Model.ProductDto.; i++)
                        {
                            <div class="carousel-item @(i == 0 ? "active" : "")">
                                <img src="@Model.ProductDto.Images[i].Url" class="d-block w-100" alt="Изображение @i" />
                            </div>
                        }
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#productCarousel" data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#productCarousel" data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    </button>
                </div>

                <!-- Блок размеров -->
                <div class="mt-3">
                    <h5>Размеры:</h5>
                    <div class="d-flex gap-2">
                        @{
                            var sizes = new[]
                            {
                    new { Code = "M", Quantity = Model.ProductDto.SizeM },
                    new { Code = "L", Quantity = Model.ProductDto.SizeL },
                    new { Code = "XL", Quantity = Model.ProductDto.SizeXl }
                    };
                        }

                        @foreach (var size in sizes)
                        {
                            if (size.Quantity > 0)
                            {
                                <button class="btn btn-outline-dark">@size.Code</button>
                            }
                            else
                            {
                                <button class="btn btn-outline-secondary" disabled>@size.Code</button>
                            }
                        }
                    </div>
                </div>

                <!-- Блок цветов -->
                <div class="mt-3">
                    <h5>Цвета:</h5>
                    <div class="d-flex gap-2">
                        @{
                            var colors = new[]
                            {
                    new { Name = "Черный", Code = "#000000", Available = true },
                    new { Name = "Белый", Code = "#FFFFFF", Available = true },
                    new { Name = "Красный", Code = "#FF2000", Available = false },
                    new { Name = "Синий", Code = "#0000FF", Available = true },
                    new { Name = "Зеленый", Code = "#00FF00", Available = false }
                    };
                        }

                        @foreach (var color in colors)
                        {
                            <button class="btn p-0 rounded-circle border-2 color-circle"
                                    style="width: 40px; height: 40px; background-color: @color.Code !important; @(color.Available ? "" : "opacity: 0.5")"
                                    disabled="@(!color.Available)"
                                    title="@color.Name">
                            </button>
                        }
                    </div>
                </div>
            </div>

            <!-- Информация о товаре -->
            <div class="col-md-6 mt-4 mt-md-0">
                <h2>@Model.Product.Name</h2>
                <h4>@($"{Model.Product.Price:C}")</h4>
            </div>
        </div>
    </div> *@
}
